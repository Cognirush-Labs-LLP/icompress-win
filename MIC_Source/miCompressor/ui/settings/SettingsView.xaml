<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="miCompressor.ui.SettingsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:miCompressor.ui"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <StackPanel VerticalAlignment="Stretch" Orientation="Vertical" Margin="10,30,20,10" Width="270">
            <local:QualitySettings />
            <Border Style="{StaticResource LineSeparatorStyle}" />
            <local:DimensionSettingsView />
            <Border Style="{StaticResource LineSeparatorStyle}" />
            <local:OutputFileSettingsView />
            <Border Style="{StaticResource LineSeparatorStyle}"/>
            <local:OutputFolderSettingsMiniView />
        </StackPanel>
    </Grid>
</Page>
