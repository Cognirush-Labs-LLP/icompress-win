<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="miCompressor.ui.SettingsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:miCompressor.ui"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <ScrollView>
            <Border BorderThickness="2" CornerRadius="16" HorizontalAlignment="Stretch" BorderBrush="{StaticResource Primary_10}" Margin="4" Padding="0" >

                <StackPanel VerticalAlignment="Stretch" Orientation="Vertical" Margin="10,30,10,10" Width="250">
                    <local:QualitySettings />
                    <Border Style="{StaticResource LineSeparatorStyle}" />
                    <local:DimensionSettingsView />
                    <Border Style="{StaticResource LineSeparatorStyle}" />
                </StackPanel>
            </Border>
        </ScrollView>
    </Grid>
</Page>
