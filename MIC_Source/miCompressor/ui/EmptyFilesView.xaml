<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="miCompressor.ui.EmptyFilesView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:miCompressor.ui"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ControlAltFillColorTransparent}">
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border BorderThickness="2" BorderBrush="Gray" CornerRadius="10" Padding="20" HorizontalAlignment="Center" VerticalAlignment="Center" Grid.Row="0" >
            <StackPanel Orientation="Vertical" HorizontalAlignment="Center" Spacing="10">
                <TextBlock Text="Compress, Convert &amp; Resize Images/Photos in Bulk"
                    FontSize="14"
                    Foreground="DarkGray"
                    TextAlignment="Center"/>
                <TextBlock Text="Drag &amp; Drop Images or Folders"
                    FontSize="34"
                    FontWeight="Thin"
                    TextAlignment="Center"/>

            </StackPanel>
        </Border>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Grid.Row="1" Spacing="5">
            <TextBlock FontSize="12" FontWeight="SemiBold" Opacity="0.5">Supported Extensions</TextBlock>
            <TextBlock Text="{x:Bind SupportedExtensionsInCaps}" FontSize="12" FontWeight="Normal" Opacity="0.8"/>
        </StackPanel>

    </Grid>
</UserControl>
